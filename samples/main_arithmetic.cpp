#include "stack.h"
#include "arithmetic.h"

void main()
{
	Stack<int> st(2), st1(st);
	bool b;
	int a;

	//for (int i = 0; i < 5; i++)
	//	st.Push(i + 1);
	//for (int i = 0; i < 6; i++)
	//	st;
	//st.Push(2);
	//for (int i = 0; i < 6; i++)
	//	st;
	//a = st.Amount_Elemnts();
	//a = st.Pop();
	//a = st.Amount_Elemnts();
	//a = st1.Amount_Elemnts();
	//b = st.Empty();
	//b = st1.Empty();
	//st.Push(1);
	//a = st.Peek();
	//a = st.Peek();
	//a = st.Peek();
	//for (int i = 0; i < 8; i++)
	//	st;
	//a = st.Pop();
}
